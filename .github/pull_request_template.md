## What this changes

<!-- One or two sentences. If it fixes an issue, link it. -->

## How it was verified

<!--
`pytest` and `ruff check src tests packaging` are the baseline and CI runs both.
If this touches mode selection or the vendor catalogue, say what hardware or
what software you tested against — those paths are where a change can look
correct and still be wrong on real devices.
-->

- [ ] `pytest` passes
- [ ] `ruff check src tests packaging` is clean
- [ ] Tested on real hardware (say which, if so)

## Notes

<!--
Anything a reviewer would otherwise have to work out: a controller quirk, a
Windows API detail, why the obvious approach doesn't work.
-->
